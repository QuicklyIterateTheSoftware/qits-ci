# THIS FILE IS GENERATED BY OpenApiSchemaExportTest — DO NOT EDIT MANUALLY.
# Run `./mvnw -pl service -am test -Dtest=OpenApiSchemaExportTest -Dsurefire.failIfNoSpecifiedTests=false` to regenerate.
---
openapi: 3.1.0
paths:
  /ci/api/runs/{runId}/cancel:
    post:
      summary: Cancel a running CI run
      parameters:
      - name: runId
        in: path
        required: true
        schema:
          type: string
      responses:
        "202":
          description: The run's container has been asked to stop
        "404":
          description: No such run
        "409":
          description: "The run is not running, so there is nothing to stop"
      tags:
      - Ci Run Controller
info:
  title: qits-ci API
  version: 1.0.0-SNAPSHOT
